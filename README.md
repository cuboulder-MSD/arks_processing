# arks_processing

This repo contains code to retrieve and mint ARKs.

**getARKs.py**

This script pulls down all ARKs that are currently in the server, and outputs them in a csv ```arks_[current date].csv```

```python getARKs.py```

